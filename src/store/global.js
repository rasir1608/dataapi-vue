const state = {
  // 全局loading界面
  loading: false,
};
const mutations = {
  SET_LOADING(state, loading) {
    state.loading = loading;
  },
};
const actions = {
};
const getters = {
};

export default{
  state, mutations, actions, getters,
};
